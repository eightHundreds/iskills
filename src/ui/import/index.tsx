/**
 * Import UI package: candidate multi-select + import confirmation.
 * Protocol types in `types.ts`. Hosts mount via static Layer/Modal (or in-tree hooks).
 */
export { ImportReview } from './review.js';
export { SkillMultiSelect, type SkillOption } from './skill-select.js';
export type { ImportReviewItem, ImportReviewResult } from './types.js';
